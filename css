/* Estilo geral para o cabeçalho */
.header {
    background-color: #333; /* Tom escuro para o cabeçalho */
    padding: 10px 0;
    text-align: center;
}

.header-content {
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 20px;
}

/* Alinhe os itens dentro do header */
.nav, .icons {
    display: flex;
    gap: 15px; /* Espaçamento entre os itens do nav e dos ícones */
}

.site-name {
    font-size: 24px;
    font-weight: bold;
    color: #fff;
}
